const noSelectionOption = '<option value="">[select a region]</option>';

export function setupCascadingUserInputs({
    countryDataList,
    countryInput,
    hiddenInput,
    cityDataList,
    stateContainer,
    stateSelect
}) {

}
